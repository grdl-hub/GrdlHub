{
  "name": "grdlhub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A Progressive Web App hub with Firebase authentication and role-based access control",
  "keywords": [
    "pwa",
    "firebase",
    "vite",
    "javascript",
    "hub",
    "dashboard"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy",
    "setup-firebase": "node scripts/setup-firebase.js",
    "verify-firebase": "node scripts/verify-firebase-config.js",
    "check-firebase": "node scripts/check-firebase-config.js",
    "debug": "node scripts/debug.js"
  },
  "devDependencies": {
    "firebase": "^12.2.1",
    "vite": "^7.1.2",
    "vite-plugin-pwa": "^1.0.3"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "idb": "^8.0.3"
  }
}
