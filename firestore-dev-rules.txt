rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary development rules - allow all authenticated users full access
    // ⚠️ WARNING: These are permissive rules for development only
    // ⚠️ You should restrict these in production
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
